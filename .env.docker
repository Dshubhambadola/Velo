# Database Configuration
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=velo
POSTGRES_PASSWORD=password
POSTGRES_DB=velo

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# Application Configuration
PORT=8080
GIN_MODE=release
# Public URL of the frontend application (browser access)
FRONTEND_URL=http://localhost:80

# Security
JWT_SECRET=development-secret-key-do-not-use-in-production

# Email Service (SendGrid)
# Leave empty to use mock logger
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=no-reply@velo.finance
SENDGRID_FROM_NAME=Velo Finance

# SSO Configuration (Google)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URL=http://localhost:8080/auth/sso/callback

# Payment Provider (Circle)
# Leave empty to use simulation mode
CIRCLE_API_KEY=
CIRCLE_SANDBOX=true

# Compliance (Sumsub)
# Leave empty to use simulation mode
SUMSUB_APP_TOKEN=
SUMSUB_SECRET_KEY=
SUMSUB_LEVEL_NAME=basic-kyc-level
SUMSUB_SIMULATION=true
